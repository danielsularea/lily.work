doc
html(lang="en")
  | {% include head.html %}
  body.post
    .post_bg.index
      .g--grid-fixed
        | {% include nav.html %}
        | {% include projects.html %}

        h2 Writing
        | {% for post in site.categories["writing"] %}
        .article
          a(href="{{ post.url }}")
            .article_title {{ post.title }}
            .article_year {{ post.date | date: "%Y" }}
        | {% endfor %}
      a.post_bg_overlay(href="/")
      a.post_close(href="/") &uarr; Back
    .post_content.g--grid
      .g--grid_row
        h3.post_date {{ page.date | date: "%b %d, %Y" }}
        h1 {{ page.title }}
        | {{ content }}
    | {% include scripts.html %}